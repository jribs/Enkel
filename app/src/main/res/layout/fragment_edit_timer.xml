<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    tools:context=".ui.EditTimerFragment">

    <EditText

        android:id="@+id/field_edit_timer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Time in seconds"
        android:inputType="number" />


    <LinearLayout
        android:id="@+id/dialog_container_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@+id/field_edit_timer">

        <Button
            android:id="@+id/button_dialog_cancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="5"
            android:text="@string/cancel"
            android:textColor="@color/colorPrimary"
            android:background="#80ffffff"
            />

        <Button
            android:id="@+id/button_dialog_save"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="5"
            android:text="@string/save"
            android:textColor="@color/colorAccent"
            android:background="#80ffffff"
            />

    </LinearLayout>


</android.support.constraint.ConstraintLayout>